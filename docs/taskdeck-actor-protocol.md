# TaskDeck Actor Protocol

The previous AI-agent actor definitions and role-specific control-plane guidance have been removed because this area is being redesigned.

Until the redesign lands, this file is only a placeholder. Do not infer durable actor responsibilities, manager authority, worker authority, parent/sub-session behavior, or agent-to-agent communication rules from the removed model.

Any new actor protocol must be introduced explicitly with the redesign.
