{
  "agentProfiles": [
    {
      "id": "codex",
      "label": "Codex",
      "command": "replace with your Codex launch command",
      "description": "Example placeholder. Put machine-local profile commands in taskdeck.local.json or a TASKDECK_CONFIG file."
    },
    {
      "id": "taskdeck-manager",
      "label": "TaskDeck Manager",
      "command": "replace with your manager launch command",
      "description": "Example placeholder for a dedicated manager session. Keep manager sessions rooted at the TaskDeck control/document root."
    },
    {
      "id": "zsh-host",
      "label": "zsh - host",
      "command": "zsh",
      "description": "Run zsh on the host in the selected project cwd."
    },
    {
      "id": "codex-app-server",
      "label": "Codex App Server (persistent auth)",
      "command": "sh -lc 'export CODEX_HOME=\"$HOME/.taskdeck-codex-home\"; mkdir -p \"$CODEX_HOME\"; exec codex app-server --listen stdio://'",
      "description": "Development example: run Codex App Server with a stable local CODEX_HOME so normal ChatGPT login auth can persist across TaskDeck restarts."
    }
  ]
}
