{
  "agentProfiles": [
    {
      "id": "codex-app-server",
      "label": "Codex App Server",
      "command": "codex --sandbox danger-full-access --ask-for-approval never app-server --listen stdio://",
      "description": "Primary App Server route. Put machine-local overrides for this profile in taskdeck.local.json or a TASKDECK_CONFIG file."
    }
  ]
}
