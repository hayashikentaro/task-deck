{
  "name": "task-deck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/server",
    "apps/web",
    "packages/core"
  ],
  "scripts": {
    "dev": "npm --workspace @taskdeck/server run dev",
    "build": "npm --workspace @taskdeck/web run build",
    "start": "npm --workspace @taskdeck/server run start"
  }
}
